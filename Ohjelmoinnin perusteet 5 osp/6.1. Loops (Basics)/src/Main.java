public class Basics {

	public static void main(String[] args) {
	
	// 1 - For:

        for (int laskuri = 0 ; laskuri < 5 ; laskuri ++)
        {
		System.out.println(laskuri);
        }

	// 2 - For:

	    for (int laskuri = 10 ; laskuri <= 20 ; laskuri ++)
        {
		System.out.println(laskuri);
        }
	
	// 3 - For:

		for (int laskuri = 0 ; laskuri <= 20 ; laskuri ++)
		
		if (laskuri == 5 || laskuri == 16 || laskuri == 20)
		{
		System.out.println(laskuri);
		}
	
	// 4 - For:

		for (int laskuri = 0 ; laskuri <= 20 ; laskuri ++)
		
		if (laskuri == 5 || laskuri == 16 || laskuri == 20)
		{
		System.out.println(laskuri);
		}

	// 5 - For:

		for (int laskuri = 0 ; laskuri < 5 ; laskuri ++)
		{
		System.out.println("loop");
		}

	// 6 - While:
		
		int counter = 1;
			
		while (counter <= 5)
		{
			System.out.println(counter);
			counter++;
		}
	
	// 7 - For:

		for (int i = 1 ; i < 11 ; i++)
		{
			System.out.print("*");
		}

	// 8 - For:
		for (int i = 1 ; i < 11 ; i++)
		{
			System.out.println("Round " + i);
		}


	}

}
